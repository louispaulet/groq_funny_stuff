export function FlagFoundryDetail() {
  return (
    <>
      <p>
        🚩 Flag Foundry adapts the SVG Prompt Lab flow for vexillology fans. It now covers every sovereign flag,
        grouped by continent. Europe auto-queues while the rest wait for your click so you can pace the
        <code className="rounded bg-slate-900/80 px-1 py-0.5 text-[0.7rem] text-white">/svg</code> route two seconds at a
        time.
      </p>
      <p>
        Each card pairs the AI generated SVG with a Unicode-rendered reference flag using
        <code className="rounded bg-slate-900/80 px-1 py-0.5 text-[0.7rem] text-white">react-country-flag</code>, making it
        easy to compare palettes and ratios while the gallery fills in real time—whether you trigger a single region or
        the entire planet. 🌍
      </p>
    </>
  )
}
